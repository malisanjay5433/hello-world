import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.helloworld.com',
  appName: 'hello-world',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
